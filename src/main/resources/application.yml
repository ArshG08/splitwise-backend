spring:
  datasource:
    url: jdbc:mysql://localhost:3306/splitwise_db
    username: root
    password: ArshArsh@1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ThisIsMyVerySecretKeyForSplitwiseApplication2026ThisShouldBeLongEnough
  expiration: 86400000

server:
  port: 8080